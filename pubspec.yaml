name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  http: ^1.2.1
  firebase_auth: ^4.20.0 # 🚨 แก้ไขตรงนี้: ใส่เลขเวอร์ชันจริง
  firebase_core: ^2.32.0  # ใช้เวอร์ชันล่าสุดที่คุณมี
  cloud_firestore: ^4.17.5 # ใช้เวอร์ชันล่าสุดที่คุณมี
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter
  # iOS-style icons
  cupertino_icons: ^1.0.8

  # LINE Login
  flutter_line_sdk: ^2.6.1
  fl_chart: ^0.66.0

  # Google Login
  google_sign_in: ^6.1.6

  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.2

  flutter_secure_storage: ^9.2.2
  jwt_decoder: ^2.0.1
  firebase_messaging: ^14.9.4
  device_info_plus: ^10.1.2
  uuid: ^4.5.1
  google_fonts: ^8.0.1
  image_picker: ^1.1.2
  url_launcher: ^6.3.2
  path: ^1.9.0
<<<<<<< Updated upstream

=======
  permission_handler: ^11.4.0
  geolocator: ^12.0.0
  geocoding: ^4.0.0
  html: ^0.15.6
>>>>>>> Stashed changes

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Recommended lints
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo_finance_care.png
    - assets/line_icon.png
    - assets/